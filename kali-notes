umask
666-files
777-directories
lastlog-previous entries
exporting or adding path 
export PATH=$PATH:$HOME/scripts
adding to front
export PATH=$HOME/scripts:$PATH



STAT
R-running
S-sleeping
D-uninterruptible sleep waiting for i/o
Z-zombie -finished but not cleaned up by parent
T-stopped/terminated
I-idle



MODIFIER FLAGS
s-session leader-started its own session
l-multi-threaded
+-in the foreground
< -high priority
N-low priority(nice)



ps aux
ip a
ip r- route taken within ur local system
traceroute -hops in traffic
